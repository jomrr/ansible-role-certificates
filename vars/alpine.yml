---
# name: ansible-role-certificates
# file: vars/alpine.yml

certificates_ca_command: /usr/sbin/update-ca-certificates

certificates_ca_trust_dir: /usr/local/share/ca-certificates

certificates_tls_dir: /etc/ssl
